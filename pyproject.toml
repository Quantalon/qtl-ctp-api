[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cmake",
    "scikit-build",
    "ninja; platform_system!='Windows'",
    "pybind11>=2.7.1",
]

build-backend = "setuptools.build_meta"
